# Strava API app credentials
# Found at: https://www.strava.com/settings/api
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret

# Path to store OAuth tokens (default: ~/.strava-tokens)
# The file is created with chmod 600 for security.
STRAVA_TOKEN_FILE=~/.strava-tokens

# OAuth redirect URI -- must match what's registered in your Strava API app
# STRAVA_REDIRECT_URI=http://localhost:9876/callback
